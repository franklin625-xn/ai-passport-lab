# Backlog

- Add a small asset manifest so future characters can replace the four frame
  files without editing the demo logic.
- Add separate sound effects for the two actions.
- Add a score or reaction counter only after the core device loop is stable.
